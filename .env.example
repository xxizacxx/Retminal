# ===================================================================
#   MODELE de secrets pour Retminal
#   -> COPIE ce fichier en ".env" (ou "secret.env" sur Mac)
#      et remplace les valeurs par LES TIENNES.
#   -> Ton vrai .env n'est JAMAIS publie (il est dans le .gitignore).
# ===================================================================

# --- Ton serveur VPS ---
VPS_HOST=1.2.3.4
VPS_USER=root
VPS_PASSWORD=ton-mot-de-passe-ici
VPS_PORT=22

# --- Claude Code (genere le code avec :  claude setup-token) ---
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-colle-ton-token-ici
